# Styles Directory

This folder is reserved for design tokens, shared Tailwind presets, or utility classes that should be documented outside of the `app` directory. Add additional files here as the Meeting Cost Timer visual system grows.
